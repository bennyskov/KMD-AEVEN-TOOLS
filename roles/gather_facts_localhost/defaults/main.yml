---
# Default variables for gather_facts_localhost role

# Control which information is gathered and displayed
show_debug_facts: false
collect_mountpoints: true
save_to_file: true

# Output directory for saved files
output_dir: "cleanup_results"
